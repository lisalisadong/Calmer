<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainHomActivity"
    android:background="#9db1ff">

    <Switch
        android:layout_width="wrap_content"
        android:layout_height="65dp"
        android:id="@+id/onOffSwitch"
        android:layout_gravity="center_horizontal|bottom"
        android:checked="false"
        android:enabled="true" />

    <TextView

        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Calmly"
        android:id="@+id/titleTextView"
        android:layout_gravity="center_horizontal|top"
        android:textSize="150dp"
        android:textColor="#FFFFFF" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center">

        <Button
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:text="Calibration"
            android:id="@+id/calibrationButton"
            android:layout_centerVertical="true"
            android:layout_centerHorizontal="true" />

        <Button
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:text="View Heart Rate"
            android:id="@+id/viewHeartrateButton"
            android:layout_below="@+id/calibrationButton"
            android:layout_alignStart="@+id/calibrationButton" />

        <Button
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:text="View Trends"
            android:id="@+id/viewTrendsButton"
            android:layout_below="@+id/viewHeartrateButton"
            android:layout_alignStart="@+id/viewHeartrateButton" />
    </RelativeLayout>

</FrameLayout>